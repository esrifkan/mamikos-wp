## Installation

1. Extract the zip file and copy the `throttle-login` folder into your plugins directory into its own folder.
2. Activate the plugin in the Plugins options.
3. Customise thr settings from the Options panel, if desired.